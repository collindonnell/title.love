<nav>
  <div class="navbar bg-base-300">
    <div class="flex-1">
        <%= button_to "title.love", root_path, class: "btn btn-ghost text-xl" %>
    </div>
    <div class="flex-none">
      <% if logged_in? %>
        <%= link_to "Logout", session_path, data: {turbo_method: :delete}, class: "btn" %>
      <% else %>
        <%= link_to "Sign Up", new_user_path, class: "btn btn-secondary" %>
        <%= link_to "Login", new_session_path, class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</nav>
