<nav>
  <div class="navbar bg-base-300">
    <div class="flex-1 ml-4">
      <div class="flex items-baseline gap-2">
        <%= link_to root_path, class: "text-xl font-semibold text-black hover:underline" do %>
          title.love
        <% end %>
        <span class="text-sm text-gray-600">enter a title, have it cased.</span>
      </div>
    </div>
    <div class="flex-none">
      <% if logged_in? %>
        <%= link_to "Logout", session_path, data: {turbo_method: :delete}, class: "btn" %>
      <% else %>
        <%= link_to "Sign Up", new_user_path, class: "btn btn-secondary" %>
        <%= link_to "Login", new_session_path, class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</nav>
